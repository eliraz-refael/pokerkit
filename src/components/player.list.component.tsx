import * as React from 'react';
const { PureComponent } = React;

export default class PlayerListComponent extends PureComponent {

	public render(): React.ReactElement<any> {
		return <div>list of players</div>;
	}
}